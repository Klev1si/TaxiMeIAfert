<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />
    <uses-permission android:name="android.permission.ACCESS_COARSE_LOCATION" />
    <!-- Required for GPS to continue when driver locks screen / switches apps -->
    <uses-permission android:name="android.permission.ACCESS_BACKGROUND_LOCATION" />
    <!-- Required to run an Android Foreground Service -->
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE_LOCATION" />
    <!-- FCM push notifications (Android 13+) -->
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS" />

    <!-- Explicitly remove the advertising-ID permissions that Firebase / Play
         Services libraries auto-add via manifest merging. We don't show ads,
         so claiming we use an advertising ID would be incorrect. Both the
         legacy Play Services AD_ID and the Android 13+ Privacy Sandbox
         AD_ID need a tools:node="remove" — Play Console treats either as
         "uses advertising ID" and would otherwise block the release. -->
    <uses-permission android:name="com.google.android.gms.permission.AD_ID" tools:node="remove" />
    <uses-permission android:name="android.permission.ACCESS_ADSERVICES_AD_ID" tools:node="remove" />

    <application
      android:name=".MainApplication"
      android:label="@string/app_name"
      android:icon="@mipmap/ic_launcher"
      android:roundIcon="@mipmap/ic_launcher_round"
      android:allowBackup="false"
      android:theme="@style/AppTheme"
      android:networkSecurityConfig="@xml/network_security_config"
      android:supportsRtl="true">

      <!-- Google Maps API key — hardcoded to guarantee it survives dotenv issues -->
      <meta-data
        android:name="com.google.android.geo.API_KEY"
        android:value="AIzaSyAYBI5oVv2TnqtysGBsIbWx4kvupAEiGEE"/>

      <!-- FCM default notification channel — must match the channel created in
           MainApplication.kt (HIGH importance so notifications wake the screen).
           tools:replace overrides the empty default declared by
           react-native-firebase/messaging's manifest. -->
      <meta-data
        android:name="com.google.firebase.messaging.default_notification_channel_id"
        android:value="taxiapp_rides"
        tools:replace="android:value"/>

      <!-- Driver-online GPS foreground service. Keeps the JS process alive and
           location updates flowing while the driver is online, even with the
           screen locked or the app backgrounded. -->
      <service
        android:name="com.taximelafert.location.LocationForegroundService"
        android:foregroundServiceType="location"
        android:exported="false" />

      <activity
        android:name=".MainActivity"
        android:label="@string/app_name"
        android:theme="@style/SplashTheme"
        android:configChanges="keyboard|keyboardHidden|orientation|screenLayout|screenSize|smallestScreenSize|uiMode"
        android:launchMode="singleTask"
        android:windowSoftInputMode="adjustResize"
        android:exported="true">
        <intent-filter>
            <action android:name="android.intent.action.MAIN" />
            <category android:name="android.intent.category.LAUNCHER" />
        </intent-filter>

        <!-- Deep-link URL scheme: taxiapp://  -->
        <!-- Used by FCM notification taps to resume the correct screen -->
        <intent-filter android:label="TaxiApp deep link">
            <action android:name="android.intent.action.VIEW" />
            <category android:name="android.intent.category.DEFAULT" />
            <category android:name="android.intent.category.BROWSABLE" />
            <data android:scheme="taxiapp" />
        </intent-filter>
      </activity>
    </application>
</manifest>
