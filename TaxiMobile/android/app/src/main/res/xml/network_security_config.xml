<?xml version="1.0" encoding="utf-8"?>
<!--
  Android Network Security Configuration
  https://developer.android.com/training/articles/security-config

  • Release builds: HTTPS only for all traffic (default)
  • Debug builds: cleartext allowed to emulator host (10.0.2.2) and
    localhost only — never to external domains.

  Applied via android:networkSecurityConfig in AndroidManifest.xml.
  This replaces the deprecated android:usesCleartextTraffic manifest attribute.
-->
<network-security-config>

    <!-- All domains not listed below must use HTTPS (default for API 28+) -->
    <base-config cleartextTrafficPermitted="false">
        <trust-anchors>
            <!-- Trust the system CAs (real certificates) -->
            <certificates src="system"/>
        </trust-anchors>
    </base-config>

    <!-- Debug-only: allow cleartext to the Android emulator host and localhost.
         This block is ignored in release builds. -->
    <debug-overrides>
        <trust-anchors>
            <!-- Also trust user-installed CAs in debug (Charles/mitmproxy etc.) -->
            <certificates src="system"/>
            <certificates src="user"/>
        </trust-anchors>
    </debug-overrides>

    <!-- Emulator: 10.0.2.2 = host machine localhost — needed for dev API server -->
    <!-- Also allows local WiFi IP for real device testing on same network    -->
    <domain-config cleartextTrafficPermitted="true">
        <domain includeSubdomains="false">10.0.2.2</domain>
        <domain includeSubdomains="false">localhost</domain>
        <domain includeSubdomains="false">127.0.0.1</domain>
        <domain includeSubdomains="true">192.168</domain>
    </domain-config>

</network-security-config>
