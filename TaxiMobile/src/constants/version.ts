/**
 * Current app version — must match the version declared in:
 *   android/app/build.gradle  (versionName)
 *   ios/TaxiMobile/Info.plist  (CFBundleShortVersionString)
 *
 * Update this constant whenever you bump the native build version so the
 * API version-check endpoint can correctly determine upgrade eligibility.
 */
export const APP_VERSION = '1.0.0';
